let LOG_COUNT = 0
export const logLine = (centerMsg: any = LOG_COUNT) => {
  console.log(
    `______________________________________________________________________________________________________________________________________${centerMsg}`
  )
}
